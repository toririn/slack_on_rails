$('#task_list').html("<%=j render(partial: 'task_list', locals: { task_list: @task_list } ) %>")
<% if @result_delete %>
  alert("削除が完了しました。反映には時間がかかるのでしばらく待ってから更新してください。");
<% else %>
  alert("削除に失敗しました。やり直すか直接Slack上で削除してください。ごめんなさい。");
<% end %>
